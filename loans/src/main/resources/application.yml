

spring:
  datasource:
    url: jdbc:mysql://localhost:3308/loansdb
    username: root
    password: root

  profiles:
    active: "prod"

  application:
    name: "loans"

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  sql:
    init:
      mode: always
  config:
    import: "optional:configserver:http://localhost:8071/"

  rabbitmq:
    host: "localhost"
    port: 5672
    username: "guest"
    password: "guest"

server:
  port: 8191

management:
  endpoints:
    web:
      exposure:
        include: "*"



